export default function Home() {
  return (
    <main className="mx-auto max-w-[1280px] px-6 py-40">
      <p className="type-meta">blockout // compiling…</p>
      <h1 className="type-display text-6xl mt-4">The Journey</h1>
      <p className="mt-6 max-w-[65ch] text-muted-foreground">
        Built in Phase 6 from design/SITEMAP.md — this shell exists so the
        scaffold smoke test renders on-brand.
      </p>
    </main>
  );
}
